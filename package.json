{
  "name": "permiso-platform",
  "version": "1.0.0",
  "description": "AI-powered permit management and automation system",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "vercel-build": "cd client && npm install && npm run build",
    "vercel-build-server": "cd server && npm install"
  },
  "keywords": ["permit", "management", "ai", "automation", "mern"],
  "author": "Permiso Platform",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
